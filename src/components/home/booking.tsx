import { FC } from 'react'
import { StyledButton } from '@/components/buttons'

const HomeBookingBtn: FC = () => {
  return (
    <StyledButton color="primary" size="large" variant="contained">
      Book Session
    </StyledButton>
    )
}

export default HomeBookingBtn
